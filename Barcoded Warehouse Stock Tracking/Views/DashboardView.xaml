<UserControl x:Class="Barcoded_Warehouse_Stock_Tracking.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Dashboard Cards -->
            <UniformGrid Columns="3" Height="120" Margin="0,0,0,30">
                <!-- Card 1 -->
                <Border Background="#3498db" CornerRadius="12" Margin="0,0,20,0" Padding="20">
                    <Grid>
                        <TextBlock Text="📦 Toplam Ürün" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Top"/>
                        <TextBlock x:Name="lblTotalProducts" Text="0" Foreground="White" FontSize="36" FontWeight="Bold" VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>
                <!-- Card 2 -->
                <Border Background="#2ecc71" CornerRadius="12" Margin="0,0,20,0" Padding="20">
                    <Grid>
                        <TextBlock Text="💰 Bugünkü Satış" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Top"/>
                        <TextBlock x:Name="lblDailySales" Text="₺0.00" Foreground="White" FontSize="36" FontWeight="Bold" VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>
                <!-- Card 3 -->
                <Border Background="#e74c3c" CornerRadius="12" Padding="20">
                    <Grid>
                        <TextBlock Text="⚠ Kritik Stok (&lt;5)" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Top"/>
                        <TextBlock x:Name="lblLowStock" Text="0" Foreground="White" FontSize="36" FontWeight="Bold" VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>
            </UniformGrid>

            <!-- Action Areas -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Add / Edit Product -->
                <Border Grid.Column="0" Background="{StaticResource BgCard}" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Ürün Ekle / Güncelle" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="txtBarcode" Grid.Column="0" Tag="Barkod (Okutun)" Margin="0,0,10,0" TextChanged="TxtBarcode_TextChanged"/>
                            <TextBox x:Name="txtPrice" Grid.Column="1" Tag="Birim Fiyat" />
                        </Grid>
                        <TextBox x:Name="txtName" Tag="Ürün Adı" Margin="0,0,0,15"/>
                        <Button Content="KAYDET / GÜNCELLE" Style="{StaticResource PrimaryButton}" Click="BtnAddProduct_Click"/>
                    </StackPanel>
                </Border>

                <!-- Manual Stock Movement -->
                <Border Grid.Column="2" Background="{StaticResource BgCard}" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Hızlı Stok Hareketi" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        <TextBox x:Name="txtMovementBarcode" Tag="Barkod (Okutun)" Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="cmbType" Grid.Column="0" Margin="0,0,10,0" SelectedIndex="0" Background="#232D4E" Foreground="White" BorderBrush="#3C5078">
                                <ComboBoxItem Content="Giriş"/>
                                <ComboBoxItem Content="Çıkış"/>
                            </ComboBox>
                            <TextBox x:Name="txtQuantity" Grid.Column="1" Tag="Miktar (Örn: 10)" Text="1" />
                        </Grid>
                        <Button Content="HAREKET EKLE" Style="{StaticResource PrimaryButton}" Background="#3498db" Click="BtnAddMovement_Click"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- DataGrids -->
            <TextBlock Text="Ürün Listesi" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <Border Background="{StaticResource BgCard}" CornerRadius="12" Padding="1" Margin="0,0,0,30">
                <DataGrid x:Name="dgvProducts" AutoGenerateColumns="False" IsReadOnly="True" Height="300" LoadingRow="DgvProducts_LoadingRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="No" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Barkod" Binding="{Binding Barcode}" Width="150"/>
                        <DataGridTextColumn Header="Ürün Adı" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Fiyat (₺)" Binding="{Binding UnitPrice, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Stok" Binding="{Binding StockQty}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <TextBlock Text="Son Stok Hareketleri" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <Border Background="{StaticResource BgCard}" CornerRadius="12" Padding="1" Margin="0,0,0,20">
                <DataGrid x:Name="dgvMovements" AutoGenerateColumns="False" IsReadOnly="True" Height="250">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tarih" Binding="{Binding CreatedAt, StringFormat=g}" Width="160"/>
                        <DataGridTextColumn Header="İşlem" Binding="{Binding Type}" Width="100"/>
                        <DataGridTextColumn Header="Barkod" Binding="{Binding BarcodeSnapshot}" Width="150"/>
                        <DataGridTextColumn Header="Miktar" Binding="{Binding Quantity}" Width="100"/>
                        <DataGridTextColumn Header="Neden" Binding="{Binding Reason}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
